<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <%= link_to restaurant.name, restaurant_path(restaurant.id), class:"font-weight-bold" %>
      (<%= restaurant.category %>)
    </li>
  <% end %>
</ul>
